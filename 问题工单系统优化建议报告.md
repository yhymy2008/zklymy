# 问题工单系统优化建议报告

**分析日期**: 2026年7月17日  
**当前系统版本**: support-mini-program v1.0  
**技术栈**: 微信小程序 + Web管理端 + Node.js/Express + SQLite (sql.js)

---

## 一、用户角色权限优化

### 1.1 当前权限体系问题

当前系统仅包含 user、staff、admin 三种角色，权限控制通过简单的角色字符串比对实现（`authMiddleware`、`staffMiddleware`、`adminMiddleware`）。这种粗粒度的权限模型存在以下问题。所有处理员（staff）拥有完全相同的权限，例如任何 staff 都可以接单、回复、解决、删除附件，无法实现按专业领域分配工单。管理员权限过大且不可细分，admin 拥有全部权限且不可撤销，缺乏"超级管理员"与"普通管理员"的区分。此外，不存在临时权限委托机制，当某个处理员休假时无法将其工单临时转交给他人。

### 1.2 推荐方案：基于 RBAC 的细粒度权限体系

建议将权限模型升级为角色-权限分离架构（RBAC），引入五个预定义角色并支持自定义角色。预定义角色包括超级管理员（super_admin，拥有系统配置、角色管理、数据删除等全局权限）、普通管理员（admin，负责用户管理、分类管理、查看报表，不可删除角色或修改系统配置）、处理员组长（staff_lead，可查看组内所有工单、批量派单、审核知识库文章、查看组内统计）、处理员（staff，可接单、回复、解决自己负责的工单）以及普通用户（user，提交工单、查看自己的工单、评价）。系统新增三张数据库表来实现这一架构：roles 表存储角色定义及其权限 JSON 字段，user_roles 表建立用户与角色的多对多映射关系，以及 depart_groups 表支持部门/小组划分。

权限粒度的设计应遵循以下原则。工单操作权限按查看、接单、回复（含内部备注）、解决、转派、关闭、删除进行细分，不同角色可为每项操作配置允许或拒绝。数据范围权限分为仅自己提交的工单、本部门工单、全部工单三个级别。管理权限涵盖用户管理、角色分配、分类管理、知识库审核发布、系统配置（SLA规则、通知模板等）、审计日志查看、数据导出等独立可配置项。

权限组合建议如下。超级管理员拥有所有权限不设限。部门主管（管理员+组长组合）可查看和分配部门内全部工单、审核知识库文章、查看部门统计报表、创建/禁用本部门成员。一线处理员仅能对自己的工单进行接单、回复、解决操作。普通用户的权限仅限于提交工单、查看自己工单、回复和评价。

### 1.3 权限实现要点

前后端应同步进行权限控制。后端每个 API 接口应增加细粒度权限校验中间件（如 `can('ticket:resolve')`），根据用户的角色权限集合判断是否放行。同时从当前 `server.js` 中移除硬编码的 `staffMiddleware` 调用，统一替换为基于权限的校验。前端菜单、按钮、操作入口应根据当前用户的权限列表动态渲染，对无权限的功能入口进行隐藏或置灰处理。权限变更后应有即时生效机制，可在登录时缓存权限快照并支持管理员手动刷新用户权限缓存。所有权限变更操作均应记录到审计日志，便于追溯。

### 1.4 其他权限相关优化

Token 安全策略方面，当前 JWT 密钥硬编码在代码中（`auth.js` 第 4 行），应改为通过环境变量注入且不设置默认值，部署时必须配置。建议增加 refresh token 机制降低 token 泄露风险，access token 有效期设为 2 小时，refresh token 有效期设为 7 天。用户修改密码或角色变更后应立即使该用户所有旧 token 失效，可维护一个 redis/memory token 黑名单或版本号机制实现。登录应增加验证码保护防止暴力破解，当前登录接口仅限流 5 次/分钟，但没有图形验证码。密码策略应强制至少 8 位且包含字母与数字的组合。

---

## 二、系统功能优化

### 2.1 工单生命周期增强

当前工单状态流转仅包含 pending、processing、resolved、closed 四种状态，缺乏中途退回和重新打开的机制。建议增加 rejected 状态允许处理员退回不符合要求的工单并填写退回原因，增加 reopened 状态允许用户对解决不满意的工单申请重新打开并附上说明。推荐完整的状态流转为：pending → processing（接单）→ resolved（解决）→ closed（用户确认关闭），中间穿插 processing → rejected（退回）→ pending（用户修改后重新提交）以及 resolved → reopened（用户不满意）→ processing（处理员继续处理）。同时应支持挂起状态 suspended 用于需要等待第三方响应或依赖外部条件的工单。

### 2.2 智能派单与负载均衡

当前工单创建后可选择指定处理人或进入待处理池，缺乏智能化分配。建议实现多种自动派单策略供管理员选择。轮询分配将新工单按顺序轮流分配给在线的处理员。最少负载分配将工单分配给当前处理中工单数量最少的处理员。技能匹配分配根据工单分类自动匹配擅长该领域的处理员（需在用户表中增加擅长领域标签字段）。均衡负载分配综合在途工单数和处理员在线状态，向工作量偏低的处理员倾斜。派单时还应考虑处理员排班，优先分配给在线状态的处理员。系统应记录每次派单原因到审计日志中。

### 2.3 SLA 时效管理

当前 SLA 规则表已创建且有默认规则（紧急 2/8 小时、高 4/24 小时、普通 8/48 小时、低 24/72 小时），但系统并未主动利用这些规则进行监控与告警。应从被动记录升级为主动监控。建议实现四级预警机制：工单创建后在响应时限的 50% 仍未接单时发送黄色提醒给处理员组长，达 80% 时升级为橙色提醒并通知管理员，超时后标记为红色告警并通过系统通知强提醒。同样，工单处理中在解决时限的 50% 和 80% 时进行分级提醒。建议增加定时任务每分钟检查一次超时工单，将紧急工单超时超过 30 分钟、其他工单超时超过 2 小时的情况自动逐级上报。

### 2.4 知识库智能关联

当前知识库功能比较基础，仅支持创建、编辑、搜索和查看。建议增加以下智能化能力。用户在提交工单时，系统根据标题和描述内容自动检索知识库中最相关的文章并在提交前展示推荐结果，引导用户先自助解决。处理员在处理工单时，系统同样在工单详情页侧边栏展示相关的知识库文章，帮助处理员快速参考。解决工单时，支持一键将处理方案沉淀为知识库草稿文章，由组长审核后发布。知识库文章应关联工单编号，实现工单与知识库文章的双向追溯。增加知识库文章的有效性反馈机制，对长时间无浏览或帮助率低的文章自动标记为待更新。

### 2.5 通知体系完善

当前仅支持站内通知，缺乏外部触达渠道。建议增加多渠道通知能力。微信小程序用户可通过订阅消息模板接收工单状态变更通知（微信小程序订阅消息能力），Web 管理端可通过浏览器通知 API 实现桌面推送，同时支持邮件通知作为兜底方式。通知偏好应允许用户自定义，如可选择仅在工单状态变更为已解决时接收通知、或订阅特定工单分类的通知。重要通知（如紧急工单超时）应支持短信通知管理员。通知内容应模板化管理，确保格式统一且便于运营调整，所有模板在前端管理界面可编辑。

### 2.6 数据统计与报表

当前统计接口（`/api/tickets/stats`）已提供基础统计数据，但缺乏可视化呈现和趋势分析。建议增加以下报表能力。处理员绩效报表包括接单数、解决数、平均解决时长、客户满意度评分均值、超时率等指标。工单趋势分析展示按日/周/月的工单量变化趋势以及分类分布趋势。问题热点分析从工单分类、关键词中自动识别高频问题类型，指导知识库建设方向。报告应支持按日期范围、处理员、分类等维度筛选，同时接入已有的 CSV 导出来源数据，便于进一步分析。前端应使用 ECharts 或 Chart.js 实现交互式可视化仪表盘。

### 2.7 批量操作扩展

当前系统已支持批量接单和批量关闭，但缺少以下常用批量操作。批量转派允许管理员选中多个工单统一转派给某个处理员，适用于处理员离职或休假交接场景。批量添加内部备注供组长对一批工单统一标注处理要点。批量导出附件支持将选中的工单所有附件打包为 ZIP 下载，方便离线查阅。批量修改优先级应在紧急事件发生时快速提升相关工单的优先级。所有批量操作需有二次确认和执行结果汇总反馈。

### 2.8 文件管理增强

当前上传文件限制为 10MB，存储在本地 `uploads/` 目录，缺少文件预览和类型安全校验。建议增加图片文件的在线预览能力（通过缩略图或弹窗查看），支持在回复中插入图片实现富文本沟通。上传文件应校验 MIME 类型白名单防止恶意文件上传，当前仅限制文件大小但未校验文件类型。对于长期无关联工单的孤立附件应支持定期清理释放存储空间。可考虑将附件存储迁移至对象存储（如腾讯云 COS）以降低服务器存储压力。

### 2.9 用户端体验优化

小程序端提交工单应选择分类后自动展示该分类的常见问题引导自助排查。工单详情页应清晰展示处理进度时间线（创建→接单→沟通→解决→评价），让用户直观了解当前处于哪个环节。增加催单功能允许用户在工单长时间无响应时发起催单提醒。Web 管理端应支持暗色模式、响应式布局适配移动端浏览器、以及键盘快捷键加快操作效率（如 Ctrl+Enter 快速发送回复）。

---

## 三、性能优化

### 3.1 数据库层面

当前使用 sql.js（纯 JavaScript SQLite 实现）在单进程中运行，存在以下性能瓶颈。每次写入操作调用 `saveDatabase()` 将整个数据库序列化为二进制写入磁盘，以 30 秒为周期的定时自动保存同样触发全量序列化。对于存在数万条工单加回复记录的场景，这种全量写入模式会显著增大 I/O 开销。

首先应启用 SQLite WAL 模式，通过 `PRAGMA journal_mode=WAL` 将写操作从阻塞读操作改为并发模式，读操作不等待写锁释放，写操作也不阻塞读操作，大幅提升并发访问性能。预计读性能提升 2-3 倍，写性能提升 3-5 倍。

当前 `saveDatabase()` 每次都将整个数据库序列化并完全覆盖写入磁盘文件。应改为增量持久化方案：对 insert/update/delete 操作采用追加日志方式记录 SQL 语句到 WAL 文件，仅在每 100 次写入或每 60 秒执行一次完整的数据库快照保存，其余时间仅依赖 WAL 文件进行崩溃恢复。这可减少约 90% 的磁盘写入量。

增加缺失的关键索引。目前 `ticket_replies` 表按 `is_deleted = 0` 过滤但该列没有索引，会导致获取回复时全表扫描；`tickets` 表按 `updated_at DESC` 排序频繁使用但该列也未建立专门索引。应添加 `CREATE INDEX idx_replies_ticket_deleted ON ticket_replies(ticket_id, is_deleted)` 和 `CREATE INDEX idx_tickets_updated_at ON tickets(updated_at)`。

引入内存缓存层减少重复查询。对于分类列表、处理员列表等读取频率高但变化极少的配置数据，引入简单的内存缓存（如 node-cache），设置 5 分钟 TTL 并在数据变更时主动失效。统计数据查询（`/api/tickets/stats`）结果可缓存 60 秒，避免每次页面加载都执行多条聚合 SQL。

COUNT 查询优化。当前列表接口每次都执行 `SELECT COUNT(*) as total` 获取总数，在数据量大时性能较差。建议对常规的列表查询（无筛选条件的默认视图）使用缓存的总数，在创建或删除工单时更新缓存。对于带筛选条件的 COUNT 查询，可考虑使用估算值（`EXPLAIN QUERY PLAN` 返回的估算行数）或利用索引覆盖的近似计数。

### 3.2 后端服务层面

静态资源服务优化。当前 Express 直接托管前端静态文件且对 JS/CSS/HTML 禁用了缓存以解决代码更新问题。正确的做法是区分生产与开发环境：开发环境禁用缓存便于调试；生产环境对带 hash 的资源文件（如 app.abc123.js）启用长期缓存（1年），对根 HTML 文件设置短期缓存或不缓存。更合理的方案是使用 nginx 作为反向代理，由 nginx 处理静态资源与 gzip 压缩、连接复用，Express 仅处理 API 请求。

接口限流优化。当前限流数据存储在进程内存的 Map 中，服务重启后丢失且无法跨进程共享。应使用外部存储如 SQLite 的内存表或独立限流计数表来持久化限流状态，确保重启后限流仍在生效且不会被绕过。限流策略应按用户角色分级：普通用户每分钟 60 次，处理员每分钟 120 次，管理员每分钟 200 次。登录和其他敏感接口单独设置更低的限流阈值。

请求日志优化。当前每条请求都调用 `console.log` 输出，在高并发场景下同步写入标准输出可能阻塞事件循环。建议仅在开发和调试阶段启用请求日志，生产环境使用异步日志库（如 pino）并将日志写入文件而非控制台。对于 ERROR 级别的日志保持即时输出以便问题排查。

连接与请求超时设置。Express 默认无请求超时限制，恶意或异常的长连接可耗尽服务资源。应使用 `server.setTimeout()` 设置全局 30 秒超时，对文件上传接口单独设置 120 秒超时。

### 3.3 前端性能优化

Web 管理端代码包体积优化。当前前端为纯原生 HTML/JS/CSS 结构，所有 JS 文件直接通过多个 script 标签加载，未做打包和压缩优化，可能存在冗余的全局变量和 DOM 查询。建议引入简单的构建工具（如 esbuild）对 JS/CSS 进行打包、Tree Shaking 和压缩，生产模式生成的 bundle 体积预计可减少 40-60%。

列表虚拟滚动。当工单列表包含数百条记录时，一次性渲染所有 DOM 节点会严重拖慢页面响应速度。建议对列表页面引入虚拟滚动技术，仅渲染可视区域内的 DOM 节点（约 20 条），滚动时动态替换节点内容。这可将大量数据列表的渲染时间从数秒降低到毫秒级。

接口请求合并与防抖。当前搜索框的每次输入可能触发接口请求，应增加 300ms 的输入防抖延迟避免无效请求。工单列表的多个筛选条件切换时应合并为单次请求而非逐个触发。列表页切换时可复用已有的列表数据缓存，返回上一页时直接从内存读取而非重新请求。

小程序端图片懒加载。工单列表和详情中展示的附件缩略图应使用小程序的 `lazy-load` 属性延迟加载，减少首屏加载时的网络请求。列表中的头像等小图应使用小尺寸 URL 而非原图。

### 3.4 数据清理与归档

工单数据随着时间积累会持续增长，影响数据库查询性能。建议实现自动归档机制：已关闭超过 90 天的工单自动迁移到归档表 `tickets_archive`，主表仅保留活跃工单，归档表不建复杂索引以节省空间。工单关联的附件中，已归档工单的附件可迁移到低成本存储或压缩归档。审计日志按 6 个月为周期滚动删除，保留必要的数据以满足合规要求即可。通知记录在标记已读 30 天后自动清理。

### 3.5 监控与告警

建议增加基础的运行监控能力。通过独立的后台线程每 60 秒检查数据库写入成功率、API 平均响应时间、内存使用量等关键指标，采用滑动窗口方式跟踪成功/失败计数。异常情况如 API 响应超时超过阈值、数据库写入连续失败、内存占用超过 80% 等应触发管理员告警通知。可暴露 `/api/health` 接口的详细信息包含数据库状态和请求统计供外部监控系统集成。

---

## 四、实施优先级建议

根据投入产出比和用户体验影响程度，建议按以下优先级分阶段实施。

第一阶段（1-2周，高优先级快速见效）：启用 SQLite WAL 模式并添加缺失的数据库索引以提升查询性能；优化 `saveDatabase()` 为增量持久化减少磁盘 I/O；增加前端防抖、虚拟滚动和图片懒加载；实现多级 SLA 超时预警与强提醒；增加登录验证码防护。

第二阶段（3-4周，核心能力补强）：实现知识库智能推荐与工单关联；增加工单退回（rejected）和重新打开（reopened）状态完善生命周期；实现多种自动派单策略和负载均衡；增加通知模板化管理与多渠道触达（微信订阅消息、邮件）。

第三阶段（5-8周，体系化升级）：升级为 RBAC 权限模型；实现交互式数据统计仪表盘与处理员绩效报表；增加 token refresh 机制与密码安全策略；完善批量操作能力；实现工单自动归档与数据清理策略。

---

## 五、参考实现要点

以下是部分关键优化的实现要点摘要，供开发时参考。

数据库连接初始化时增加 WAL 模式与缓存配置，在 `initDatabase()` 中执行 `PRAGMA journal_mode=WAL`、`PRAGMA synchronous=NORMAL`（从 FULL 降级提升写入速度）、`PRAGMA cache_size=-8000`（分配约 8MB 缓存）、`PRAGMA temp_store=MEMORY`（临时表使用内存）四条配置。

增量持久化方案的核心是在 db.js 中将 `saveDatabase()` 拆分为两个函数：`markDirty()` 仅置位脏标记而不立即写入磁盘，`flushToDisk()` 执行实际的序列化写入。调用处改为每次写操作后执行 `markDirty()`，由定时器每 60 秒触发一次 `flushToDisk()`，同时进程退出时通过 `process.on('beforeExit')` 保证最后一次数据不丢失。

RBAC 权限中间件的实现思路是定义 `checkPermission(permissionKey)` 中间件工厂函数，从 `req.user.roles` 中提取权限集合并判断是否包含目标权限。路由层将原来的 `staffMiddleware` 替换为 `checkPermission('ticket:take')` 等具体权限校验。

前端性能优化的实现方法包括：列表页引入 Intersection Observer API 实现虚拟滚动，搜索输入框包裹 debounce(fn, 300) 函数，图片标签添加 loading="lazy" 属性，构建脚本使用 esbuild 的 `--bundle --minify --tree-shaking=true` 参数。

---
*本报告基于对 system-mini-program 项目完整代码的分析（backend/server.js、backend/db.js、backend/routes/tickets.js、backend/routes/users.js、backend/middleware/auth.js 及前端/小程序代码结构），结合通用工单系统最佳实践编制。所有建议均与当前系统架构兼容，可在现有技术栈基础上逐步实施。*
