# 2026-09-15

## Git 仓库初始化（工作区 AI_coding）

- 现象：IDE Git History 面板报 `fatal: bad revision 'refs/heads/main'`，原因是有大量暂存文件但从未提交，`main` 为 unborn branch。
- 处理：
  - 新建 `.gitignore`（忽略 `node_modules/`、`__pycache__/`、`*.pyc`、`*.db`、`*.log`、编辑器目录）。
  - `git rm -r --cached` 移除两处误暂存的 `node_modules`（根目录、`support-mini-program/backend/`），共约 1476 个文件；磁盘文件保留。
  - 首次提交 `e90ca02 Initial commit`，纳入 186 个文件。
- 远端：仓库原已存在 `nhs_remote` → `https://github.com/yhymy2008/zklymy.git`；新增规范命名 `origin` 指向同一 URL，并设置 `branch.main.remote=origin` / `branch.main.merge=refs/heads/main`。尚未推送。
- 环境限制：本机访问 `github.com:443` 超时（未配置 http 代理），因此 `git ls-remote` / `git fetch` / `git push` 均失败，需先解决网络（VPN 或配置代理）。
- 待办：全局 git 身份仍是占位值 `Your Name <you@example.com>`（来自 `~/.gitconfig`），推送前建议改为真实身份并 `--amend --reset-author`。
