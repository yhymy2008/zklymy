# 2026-09-11

## 分销达人个人中心 UI 设计（daren-center-prototype）

- 交付物：`c:\Users\CNYangMe8\Documents\work\AI_coding\daren-center-prototype\index.html`（单文件、自包含、约 127KB）
- 内容：5 个 Tab 根页面（工作台 / 内容中心 / 推广·分销码 / 收益中心 / 我的）+ 8 个子页面（素材详情、专属海报、任务中心、我的团队、佣金明细、提现、资料设置、消息通知），共 13 屏；含「一键转发」底部抽屉与页面总览画廊。
- 实现方式：单 HTML + 内联 CSS + 原生 JS 导航栈（push/pop + z-index 分层）；页面总览由 JS `cloneNode` 自动生成，避免重复写 markup。
- 设计系统：品牌靛紫 #6D5DFC→#8E7BFF，激励金 #FFB020→#FF8A00，背景 #F4F5FA，文字 #151833/#5B6178/#949AB2；卡片圆角 22px，触控热区 ≥44px；图标为内联 Lucide SVG（禁用 emoji）。
- 交互要点：底部 Tab 中心「推广」凸起按钮直连分销码；内容中心素材卡 → 详情 → 一键转发（微信好友/群/朋友圈/海报/复制文案）。

## 分销达人中心 · 小程序版 UI（daren-miniprogram）

- 交付物：`c:\Users\CNYangMe8\Documents\work\AI_coding\daren-miniprogram\preview.html`（单文件、自包含、约 105KB）
- 与 H5 版（daren-center-prototype）的差异：按微信小程序规范重做 —— 原生导航栏 44pt + 胶囊按钮 87×32pt（右边距 7pt）、原生 tabBar 50pt + 底部安全区 34pt、半屏弹窗、原生 Toast（150px 宽黑底圆角）、rpx 750 栅格。
- 页面：5 个 tabBar 页（工作台 index / 内容中心 / 推广·分销码 / 收益中心 / 我的）+ 8 个二级页（素材详情、专属海报、任务中心、我的团队、佣金明细、提现、消息通知、资料设置），共 13 屏。
- 关键实现要点（已写入页面 04 章节）：onShareAppMessage 的 path 带 `sid`、小程序码 scene 解析绑定、canvas 2d 生成海报 + saveImageToPhotosAlbum、setClipboardData 复制文案。
- 重要约束：原生 tabBar **不支持凸起中心按钮**，若要做「推广」凸起需改用 custom-tab-bar 组件；本方案用原生平铺 5 Tab。
- 遗留：目录下 `_chk.py` 为临时校验脚本，删除审批超时未成功，可手动删除。

## 环境踩坑

- `ui-ux-pro-max` 插件的 `design_system.py` 第 443 行 f-string 含反斜杠，Python 3.9 下报 SyntaxError；已改为 `chr(10)` 拼接修复。
- 该脚本在 Windows 控制台输出需设置 `$env:PYTHONIOENCODING="utf-8"`，否则 GBK 编码报错。
- 本机 Python 仅 3.9.13，无 `py` 启动器。
- 单次 `write_to_file` 写入超大 HTML（>1500 行）会被中断，改为「先写基础层 + 锚点注释、再分块 `replace_in_file`」的写法。
