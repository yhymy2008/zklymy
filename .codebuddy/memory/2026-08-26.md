# 2026-08-26

- 重构 `lookup_merge.py`：A、B 两个文件均支持 .csv / .xlsx / .xlsm 格式（原先 A 仅 csv、B 仅 xlsx）
  - 新增 `iter_table()`（按扩展名自动识别，生成器逐行读取）与 `write_table()`（按输出扩展名写 csv/xlsx）
  - 新增参数：`--sheet-a`（A 为 xlsx 时的工作表）、`--b-encoding`（B 为 csv 时的编码）；`-s/--sheet` 继续作用于 B（兼容原用法）
  - 输出文件默认跟随 A 文件扩展名（原固定 `_matched.csv`）
  - 顺带修复：`--no-normalize` 原本无效（normalize_key 忽略该参数），现真正生效
  - 新增常量 CSV_EXT / EXCEL_EXTS / SUPPORTED_EXTS
- 编写回归测试 `test_lookup_merge_formats.py`（工作区根目录）：4 种格式组合 + --keep-duplicates 全部断言通过；测试中注意子进程需 env PYTHONIOENCODING=utf-8，XLSX 空单元格读回是 None 而非 ""
- 配置 Node 环境：原 `node`/`npm` 未加入 PATH。已把 `C:\Users\CNYangMe8\.workbuddy\binaries\node\versions\22.22.2`（node v22.22.2 / npm 10.9.7 / npx 10.9.7，完整安装含 npm.cmd/npx.cmd）持久化写入用户 PATH 注册表；新开终端即生效，已打开的终端/IDE 需重启生效。注意：`.workbuddy\binaries\node\versions` 下带 `installing.*.__extract_temp__` 的 18.20.4 / 20.18.0 是临时解压残留，勿用
- 修复 SonarQube for IDE (SonarLint) 报错 "Node.js runtime version 20.12.0 or later is required"：在 `%APPDATA%\Code\User\settings.json`（用户实际 IDE 为 Trae 系，读 VS Code 兼容路径）添加 `"sonarlint.pathToNodeExecutable": "C:\\Users\\CNYangMe8\\.workbuddy\\binaries\\node\\versions\\22.22.2\\node.exe"`（SonarLint 5.9.0 要求 node >22.12.0 或 24.0.0，22.22.2 满足）；修改前已备份为 settings.json.bak_20260828
